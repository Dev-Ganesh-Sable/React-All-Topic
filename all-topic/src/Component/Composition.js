import React from 'react'

// composition : Composition in React means building complex components by combining smaller, reusable components.

// Instead of inheritance, React uses composition to reuse code

function Composition() {
  return (
    <div>Composition</div>
  )
}

export default Composition